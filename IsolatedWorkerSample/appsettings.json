{
  "ApplicationInsights": {
    // no need to configure this, it only demonstrates that AppInsights worker configuration
    // should be done here and not in host.json
    // Connection string will be populated by environment variable, no need to set it here either.
    "EnableAdaptiveSampling": true,
    "EnablePerformanceCounterCollectionModule": false
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  }
}